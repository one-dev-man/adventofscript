export declare const APILocale: {
    ENDPOINTS: {
        parse(endpoint: string, argmap: {
            [key: string]: any;
        }): string;
        MAIN: string;
        readonly AUTH: string;
    };
};
